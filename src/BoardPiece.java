/**
 * An interface that purely allows the generalization of Player, RoomTile and Hallway
 * to be considered "BoardPieces".  Allows us to store all three of these objects
 * in one 2D array in board, which represents the Board for the game.
 */
public interface BoardPiece{
	
}
